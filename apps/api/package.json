{
  "name": "@ommatidia/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint rules configured for @ommatidia/api yet.'",
    "test": "npm run build --workspace @ommatidia/shared && npm run build --workspace @ommatidia/calculations && npm run build --workspace @ommatidia/extraction && npm run build --workspace @ommatidia/reports && npm run build && node --test test/*.test.mjs",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@ommatidia/shared": "0.1.0",
    "@ommatidia/calculations": "0.1.0",
    "@ommatidia/extraction": "0.1.0",
    "@ommatidia/reports": "0.1.0"
  }
}
