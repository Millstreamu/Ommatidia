{
  "name": "@ommatidia/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo 'No lint rules configured for @ommatidia/web yet.'",
    "test": "npm run build --workspace @ommatidia/web && node --test test/*.mjs",
    "start": "node dist/src/index.js"
  },
  "dependencies": {
    "@ommatidia/shared": "0.1.0"
  }
}