# Extraction provider: mock | openai
EXTRACTION_PROVIDER=mock

# Required only when EXTRACTION_PROVIDER=openai
OPENAI_API_KEY=

# Optional extraction model override for OpenAI provider
OPENAI_EXTRACTION_MODEL=gpt-4.1-mini

# Extraction runtime controls
EXTRACTION_TIMEOUT_MS=120000
EXTRACTION_MAX_RETRIES=1
