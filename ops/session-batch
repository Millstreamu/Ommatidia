#!/usr/bin/env bash
set -euo pipefail
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
echo "[session_batch] forwarding to run-session-batch.sh with args: $*"
"$SCRIPT_DIR/run-session-batch.sh" "$@"
